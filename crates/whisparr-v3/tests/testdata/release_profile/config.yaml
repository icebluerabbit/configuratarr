name: No Low-Quality Encodes
enabled: true
# Terms that must appear in release titles grabbed under this profile.
required:
  - PROPER
  - REPACK
# Terms that must NOT appear in release titles.
ignored:
  - CAM
  - TS
# 0 = apply to all indexers.
indexer_id: 0
tags: []
